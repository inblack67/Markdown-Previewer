{
  "name": "code",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "yarn build && yarn export && netlify deploy --dir=out --prod"
  },
  "dependencies": {
    "gray-matter": "^4.0.2",
    "marked": "^1.2.0",
    "next": "9.5.3",
    "react": "16.13.1",
    "react-dom": "16.13.1",
    "use-syntaxer": "^0.1.0"
  }
}
